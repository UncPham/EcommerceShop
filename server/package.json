{
  "type": "module",
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "server": "concurrently \"nodemon app.js\" \"npm run model\"",
    "build": "npm install --prefix ./client && npm run build --prefix ./client && npm install && pip install -r recommendations/requirements.txt",
    "model": "cd recommendations && python -m uvicorn main:app --host 0.0.0.0 --port 8000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.4",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^3.1.0",
    "stripe": "^15.6.0",
    "validator": "^13.12.0"
  }
}
